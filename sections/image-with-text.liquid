<section class="image-with-text section-{{ section.id }}" aria-label="{{ section.settings.heading }}">
  <div class="image-with-text__inner {% if section.settings.layout == 'text_first' %}image-with-text__inner--reverse{% endif %}">
    
    <div class="image-with-text__media">
      {%- if section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 1200
          | image_tag:
            class: 'image-with-text__img',
            alt: section.settings.image.alt | default: section.settings.heading,
            loading: 'lazy'
        }}
      {%- else -%}
        {{ 'image' | placeholder_svg_tag: 'placeholder-svg image-with-text__img' }}
      {%- endif -%}
    </div>

    <div class="image-with-text__content">
      <div class="image-with-text__content-inner">
        {%- if section.settings.subheading != blank -%}
          <p class="image-with-text__eyebrow">{{ section.settings.subheading }}</p>
        {%- endif -%}

        {%- if section.settings.heading != blank -%}
          <h2 class="image-with-text__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <div class="image-with-text__text">{{ section.settings.text }}</div>
        {%- endif -%}

        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link | default: '#' }}" class="image-with-text__btn">
            {{ section.settings.button_label }}
          </a>
        {%- endif -%}
      </div>
    </div>

  </div>
</section>

{% stylesheet %}
  .image-with-text {
    padding-block: clamp(4rem, 8vw, 8rem);
    background-color: var(--color-background);
  }

  .image-with-text__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 6rem);
    align-items: center;
  }

  .image-with-text__inner--reverse .image-with-text__media {
    order: 2;
  }

  .image-with-text__media {
    position: relative;
    border-radius: 4px;
    overflow: hidden;
    aspect-ratio: 4 / 5;
  }

  .image-with-text__img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .placeholder-svg {
    background-color: rgba(44, 36, 32, 0.05);
    color: rgba(44, 36, 32, 0.2);
    width: 100%;
    height: 100%;
    fill: currentColor;
  }

  .image-with-text__content {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .image-with-text__content-inner {
    max-width: 420px;
    margin-inline: auto;
  }

  .image-with-text__eyebrow {
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #8b7355;
    margin-bottom: 1rem;
  }

  .image-with-text__heading {
    font-family: 'Inter', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 400;
    line-height: 1.1;
    color: #2c2420;
    margin-bottom: 1.5rem;
  }

  .image-with-text__text {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: rgba(44, 36, 32, 0.8);
    margin-bottom: 2rem;
  }

  .image-with-text__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 2.5rem;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    color: #faf8f5;
    background-color: #2c2420;
    border: 1px solid #2c2420;
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  .image-with-text__btn:hover {
    background-color: transparent;
    color: #2c2420;
  }

  @media (max-width: 768px) {
    .image-with-text__inner {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .image-with-text__inner--reverse .image-with-text__media {
      order: 0;
    }

    .image-with-text__content-inner {
      text-align: center;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Image with Text",
  "class": "shopify-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Desktop image placement",
      "options": [
        {
          "value": "image_first",
          "label": "Image first"
        },
        {
          "value": "text_first",
          "label": "Image second"
        }
      ],
      "default": "image_first"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Nossa Essência"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Sobre a NYNA"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Acreditamos que a beleza mora nos detalhes. Cada acessório é pensado para realçar a feminilidade de forma leve, sofisticada e atemporal. Joias feitas para durar e te acompanhar em todos os momentos.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Conheça nossa história"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Image with Text"
    }
  ]
}
{% endschema %}
