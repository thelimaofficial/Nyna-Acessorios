<section class="featured-products section-{{ section.id }}" aria-label="{{ section.settings.heading }}">
  <div class="featured-products__header">
    {%- if section.settings.subheading != blank -%}
      <p class="featured-products__eyebrow">{{ section.settings.subheading }}</p>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2 class="featured-products__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}
  </div>

  <div class="featured-products__grid">
    {%- assign collection = section.settings.collection -%}
    {%- assign products_to_show = section.settings.products_to_show -%}

    {%- if collection != blank -%}
      {%- for product in collection.products limit: products_to_show -%}
        {% render 'product-card', product: product %}
      {%- endfor -%}
    {%- else -%}
      {%- for i in (1..products_to_show) -%}
        {% render 'product-card' %}
      {%- endfor -%}
    {%- endif -%}
  </div>

  {%- if section.settings.show_view_all and collection.all_products_count > products_to_show -%}
    <div class="featured-products__footer">
      <a href="{{ collection.url }}" class="featured-products__btn">
        {{ 'Ver todas as joias' }}
      </a>
    </div>
  {%- endif -%}
</section>

{% stylesheet %}
  .featured-products {
    padding-block: clamp(4rem, 8vw, 8rem);
    max-width: var(--page-width, 90rem);
    margin-inline: auto;
    padding-inline: var(--page-margin, 1.25rem);
  }

  .featured-products__header {
    text-align: center;
    margin-bottom: clamp(2.5rem, 6vw, 4rem);
  }

  .featured-products__eyebrow {
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #8b7355;
    margin-bottom: 0.75rem;
  }

  .featured-products__heading {
    font-family: 'Inter', sans-serif;
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 400;
    color: #2c2420;
  }

  .featured-products__grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: clamp(1rem, 3vw, 2rem);
  }

  .featured-products__footer {
    text-align: center;
    margin-top: clamp(2.5rem, 5vw, 4rem);
  }

  .featured-products__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 2.5rem;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    color: #2c2420;
    background-color: transparent;
    border: 1px solid #2c2420;
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  .featured-products__btn:hover {
    background-color: #2c2420;
    color: #faf8f5;
  }

  @media (max-width: 990px) {
    .featured-products__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .featured-products__grid {
      display: flex;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      padding-bottom: 1rem;
      margin-inline: calc(var(--page-margin, 1.25rem) * -1);
      padding-inline: var(--page-margin, 1.25rem);
      gap: 1rem;
      scrollbar-width: none; /* Firefox */
    }

    .featured-products__grid::-webkit-scrollbar {
      display: none; /* Chrome/Safari */
    }

    .featured-products__grid > * {
      flex: 0 0 75%;
      scroll-snap-align: center;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Featured Products",
  "settings": [
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Seleção Exclusiva"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Mais Vendidos"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 15,
      "step": 1,
      "default": 5,
      "label": "Maximum products to show"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "default": true,
      "label": "Show 'View all' button"
    }
  ],
  "presets": [
    {
      "name": "Featured Products"
    }
  ]
}
{% endschema %}
